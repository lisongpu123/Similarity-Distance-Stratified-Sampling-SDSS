# GitHub upload checklist

Before uploading:

- [ ] Confirm no raw patient-level identifiable data are included.
- [ ] Keep `results/archived_profile_grid/` because it is the audit trail for the manuscript table.
- [ ] Keep `scripts/build_final_sdss32_from_archived_grid.py` and `results/final_sdss32_provenance/`.
- [ ] In README, do not claim that current retraining exactly reproduces archived results unless the historical code/data snapshot is restored.
- [ ] Describe final table generation as archived profile-grid result reconstruction.
